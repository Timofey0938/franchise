import { FC } from 'react';
import styles from './FilterBlock.module.scss';

const FilterBlock: FC = () => {
  return (
    <div className={styles.filterBlock}>FilterBlock</div>
  )
}

export default FilterBlock;